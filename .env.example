# Copia este archivo como .env y rellena los valores reales
# Genera una clave con: python -c "import secrets; print(secrets.token_hex(32))"
AROLUZ_SECRET_KEY=reemplazar-con-clave-generada-de-64-caracteres-hex
